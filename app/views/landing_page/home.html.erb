<div class="row justify-content-center">
  <div class="col-9 mb-3">
    <h1>Welcome to Watto's Shop</h1>
    <h4>Here at Watto's we sell a variety of spacecraft and land vehicles from all over the galaxy</h4>
  </div>
</div>

<div class="row justify-content-center">
  <h4>Three Most Recently Added Products</h4>
</div>

<div class="row justify-content-center text-center">
  <% @products.each do |product| %>
    <div class="col-3">
      <div class="card card-body">
        <%= link_to product.name, product_path(product) %>
      </div>
    </div>
  <% end %>
</div>

<div class="row justify-content-center">
  <h4>The Most Reviewed Product</h4>
</div>

<div class="row justify-content-center text-center">
    <div class="col-3">
      <div class="card card-body">
        <%= link_to @most_reviewed_product.first.name, product_path(@most_reviewed_product.first) %>
      </div>
    </div>
</div>
